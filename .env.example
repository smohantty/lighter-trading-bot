# LIGHTER_NETWORK=mainnet (default) or testnet
LIGHTER_WALLET_CONFIG_FILE=wallet_config.json
LIGHTER_NETWORK=mainnet

# SIMULATION_MODE: Set to 'true' to enable simulation mode (no actual orders sent to exchange)
# SIMULATION_MODE=false
